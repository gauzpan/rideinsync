# Copy to .env.local and fill in. Never commit real keys.

# Canonical public URL of the hosted app (no trailing slash). Used for
# shareable invite links/QR codes. Leave blank in local dev (falls back to
# the current origin); set to the production URL in the prod build env.
VITE_APP_URL=

# Supabase (real-time backend)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Google Maps (maps, traffic, weather)
VITE_GOOGLE_MAPS_API_KEY=
# Vector Map ID (Google Cloud Console -> Maps Platform -> Map Management).
# Required for rider pins: every marker on the live map is an AdvancedMarker,
# which only renders on a vector map. Without this the map still loads
# (raster, route line intact) but no pins ever appear — easy to miss since
# there's no error, just an empty map.
VITE_MAP_ID=

# Web Push (VAPID public key — generate with `npx web-push generate-vapid-keys`;
# the matching private key is a Supabase secret set via `supabase secrets set`,
# never a client env var — see supabase/functions/push-notify/index.ts header)
VITE_VAPID_PUBLIC_KEY=

# PostHog (analytics)
VITE_PUBLIC_POSTHOG_KEY=
VITE_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

